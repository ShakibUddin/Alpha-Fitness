import React from 'react';
import NavBar from '../NavBar/NavBar';

const Header = () => {
    return (
        <div className="w-full">
            <NavBar></NavBar>
        </div>
    );
};

export default Header;